# ============================================
# PDF Print Enhance - 依赖包列表
# ============================================

# ============================================
# DocEnTR 核心依赖（严格按照原始仓库要求）
# ============================================
# 注意：torch==1.10.0 在 Python 3.11 上不可用，使用最低可用版本 2.0.0
torch==2.0.0
torchvision==0.15.0
vit_pytorch==0.24.3
einops==0.3.2
numpy>=1.22.0,<2.0.0
opencv_python>=4.6.0,<4.9.0
Pillow>=9.0.0
tqdm==4.62.3

# ============================================
# PDF 处理
# ============================================
pdf2image>=1.16.3
# 注意：还需要系统级别安装 poppler-utils
#   macOS:   brew install poppler
#   Ubuntu:  sudo apt-get install poppler-utils
#   CentOS:  sudo yum install poppler-utils

# ============================================
# 网络下载（用于模型下载脚本）
# ============================================
requests>=2.31.0
gdown>=4.7.1

# ============================================
# 开发工具（可选）
# ============================================
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0
